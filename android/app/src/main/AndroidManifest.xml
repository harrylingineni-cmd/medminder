<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required by the US Find Generic tab, which sends only the medicine
         name the user searches for to the NLM RxNav service over HTTPS. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Lets the app show notifications at all (required at runtime on
         Android 13+ / API 33+; the app must also ask the user for this,
         which NotificationService.requestPermissions() does). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Lets the app schedule a notification to fire at an exact time
         instead of Android batching/delaying it to save battery. Required
         for medication reminders to actually show up on time. On
         Android 12-13 this is granted automatically; on Android 14+
         (API 34+) the user must explicitly allow it, which is what
         NotificationService.requestPermissions() prompts for. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <!-- Lets a scheduled reminder cause a short vibration alongside the
         notification sound. -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Lets the app re-register its scheduled reminders after the phone
         reboots (Android clears all pending alarms on reboot otherwise). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:label="MediGuard"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- flutter_local_notifications: receives the alarm that fires when
             a scheduled reminder's time is reached, and shows the
             notification. Without this, scheduled notifications never
             actually appear. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />

        <!-- flutter_local_notifications: re-schedules all pending reminders
             after the phone reboots or the app is updated, since Android
             clears scheduled alarms in both cases. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required so url_launcher can find a browser to open the privacy
             policy link on Android 11+ (API 30+), which otherwise hides
             other apps from this app's package-visibility queries. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
